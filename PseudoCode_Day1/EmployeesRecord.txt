Class: Employee
Attributes: name, department
Constructor(name, department):
    SET attributes

Initialize LIST employees
Add Employee("John", "IT")
Add Employee("Sara", "HR")
Add Employee("Mike", "IT")

Function filterByDepartment(dept):
    SET found = false
    FOR each employee in employees
        IF employee.department == dept
            PRINT employee.name
            SET found = true
    IF found == false
        PRINT "No employees in " + dept

Main:
    filterByDepartment("IT")
